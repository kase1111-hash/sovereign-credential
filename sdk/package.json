{
  "name": "@sovereign-credential/sdk",
  "version": "1.0.0",
  "description": "SDK for generating zero-knowledge proofs for Sovereign Credential disclosures",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "jest",
    "lint": "eslint src --ext .ts",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "zk",
    "zero-knowledge",
    "credential",
    "nft",
    "snarkjs",
    "groth16"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "snarkjs": "^0.7.0"
  },
  "peerDependencies": {
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
