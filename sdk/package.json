{
  "name": "@sovereign-credential/sdk",
  "version": "0.1.0-alpha",
  "description": "TypeScript SDK for generating zero-knowledge proofs for Sovereign Credential",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "mocha --require ts-node/register 'test/**/*.test.ts'",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "zero-knowledge",
    "zk-proofs",
    "snarkjs",
    "circom",
    "credentials",
    "ethereum"
  ],
  "author": "kase1111-hash",
  "license": "CC0-1.0",
  "peerDependencies": {
    "ethers": "^6.0.0",
    "snarkjs": "^0.7.0"
  },
  "dependencies": {
    "circomlibjs": "^0.1.7",
    "ffjavascript": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/snarkjs": "^0.7.8",
    "ethers": "^6.11.1",
    "snarkjs": "^0.7.3",
    "typescript": "^5.4.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
